import { Fira_Code, Jost, Montserrat, Space_Grotesk } from "next/font/google";

export const spaceGrotesk = Space_Grotesk({
  subsets: ["latin"],
  display: "swap",
});

export const firaCode = Fira_Code({
  subsets: ["latin"],
  display: "swap",
});

export const jost = Jost({
  subsets: ["latin"],
  display: "swap",
});

export const montserrat = Montserrat({
  subsets: ["latin"],
  display: "swap",
});