import React from 'react';
import styles from "@/presentation/styles/components/_hero-page.module.scss";

const HeroPage = () => {
    return (
        <div>
            HeroPage
        </div>
    );
};

export default HeroPage;