import React from 'react';
import styles from "@/presentation/styles/components/_header.module.scss";

const Header = () => {
    return (
        <div>
            Header
        </div>
    );
};

export default Header;